function getTxtCoords(TXT)

    for i = 1:length(TXT)

        x = TXT{i}.Position(1);
        y = TXT{i}.Position(2);
        s = TXT{i}.String;
        fprintf('%0.2f, %4d, %s; \n',round(x,2),round(y),s)
    end

end